namespace FreelancerHub.Domain.Constants;

public static class AppSettingsSection
{
    public const string ConnectionStrings = nameof(ConnectionStrings);
    public const string RedisCacheSettings = nameof(RedisCacheSettings);
    public const string Jwt = nameof(Jwt);
}